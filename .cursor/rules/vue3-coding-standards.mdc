---
description: Vue 3 + TypeScript 企业级移动端电商项目编码规范（与 ESLint / Prettier / Commitlint 推荐集一致）
alwaysApply: true
---

# Vue 3 企业级移动端电商 — 编码规范（AI 必须遵循）

本规则与 **Vue 官方风格指南**、**TypeScript 社区主流实践**、**Feature-Sliced Design（FSD）** 目录约定一致，并与本仓库 **ESLint（`@eslint/js` + `typescript-eslint` recommended + `eslint-plugin-vue` flat/recommended + `eslint-config-prettier`）**、**Prettier 默认风格**、**Commitlint conventional** 对齐。生成或修改代码时**默认按本规则执行**，避免与上述工具链冲突。

---

## Vue 3（官方风格指南 + `<script setup>`）

- **组件名**：在 SFC 的 `defineOptions({ name: '...' })` 或文件名（见下文）中使用 **PascalCase**。
- **`data`**：Options API 若出现，`data` 必须是**返回对象的函数**；本项目优先 **Composition API + `<script setup lang="ts">`**，用 `ref` / `reactive` 代替 `data`。
- **Props**：使用 `defineProps` 时**尽量完整**：至少声明类型；优先使用**带类型的 props 声明**（`defineProps<{ ... }>()` 或 `withDefaults`），避免仅字符串数组形式。
- **`v-for`**：必须提供稳定、可追踪的 **`key`**（优先业务 id，避免用 index 作为唯一 key，除非静态列表）。
- **`v-if` 与 `v-for`**：**不要**在同一元素上同时使用；改为先 `computed` 过滤再 `v-for`，或上移 `v-if` 到包裹元素。
- **样式**：组件样式使用 **`<style scoped>`**（或与 CSS Modules / Tailwind 组合时保证样式边界清晰，不污染全局）。
- **脚本**：Vue SFC 使用 **`<script setup lang="ts">`**。
- **模板中引用组件**：**kebab-case**（如 `<product-card />`），与 PascalCase 文件名对应。
- **事件**：模板中事件名 **kebab-case**（如 `@item-click`）；脚本中处理函数命名 **camelCase**（如 `onItemClick` / `handleItemClick`）。

---

## TypeScript（严格、显式、可推断边界清晰）

- **类型/接口名**：**PascalCase**（如 `UserProfile`、`ProductSku`）。
- **变量与函数**：**camelCase**（如 `getUserById`）。
- **常量**：语义为不变配置时用 **UPPER_SNAKE_CASE**（如 `API_TIMEOUT_MS`）；导出常量对象可用 `as const`。
- **布尔变量**：使用 **`is` / `has` / `can` / `should` 前缀**（如 `isLoading`、`hasError`、`canSubmit`）。
- **禁止滥用 `any`**：优先 **`unknown`**、泛型、联合类型或具体 DTO；确需逃逸时局部收窄并注释原因。
- **严格模式**：以 **`tsconfig` 中 `strict: true`** 为准；新增代码不得引入可避免的 `@ts-ignore`。
- **函数返回类型**：公共 API、导出函数、复杂逻辑函数**显式声明返回类型**；简单内部函数可依赖推断但保持可读。
- **Props / Emits**：使用 **TypeScript 接口或字面量类型**显式定义（`defineProps<Props>()`、`defineEmits<Emits>()`），避免松散字符串事件名。

---

## 文件与目录命名

- **目录名**：**kebab-case**（如 `user-profile/`、`api-client/`）。FSD 下 `src/app`、`src/pages` 等**已有固定目录名保持不变**。
- **普通 TS 模块文件**：**kebab-case**（如 `user-service.ts`、`api-client.ts`）。
- **Vue 组件文件**：**PascalCase**（如 `ProductCard.vue`、`UserProfile.vue`）。
- **Composable 文件**：**`use` 前缀 + camelCase 余下部分**（如 `useCart.ts`、`useAuth.ts`），导出同名 composable。
- **类型声明文件**：使用 **`.d.ts`** 后缀（如 `env.d.ts`、`components.d.ts`）。

---

## 架构与工程约束（与 `README` / `docs/architecture.md` 一致）

- **HTTP 客户端**：业务与 feature 层 API **禁止**各自 `axios.create`；统一使用 **`@/shared/api/request`（或 `@/shared/api` 中导出的同一实例）** 发起请求。
- **状态管理**：使用 **Pinia**，按领域拆分 store（如 `user`、`cart`、`app`）；组件中优先 `storeToRefs` / 具名 actions，避免散落重复请求逻辑。
- **样式**：**优先 Tailwind CSS 原子类**；组件库以 **Vant** 用于交互密集场景；避免全局样式泄漏，慎改 Vant 主题时集中配置。
- **路由**：路由组件**必须懒加载**（`() => import('@/pages/...')` 或等价动态 import），禁止在路由表中静态 import 整页。

---

## Git Commit（Conventional Commits + Commitlint）

- **格式**：`<type>(<scope>): <subject>`（`scope` 可选；与 Commitlint 一致时保持全小写 type）。
- **type 枚举**：`feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `build` | `ci` | `chore` | `revert`。
- **规则**：**type 必须小写**；**subject 不以句号结尾**；**header 不超过 72 字符**；subject 非空。

---

## Prettier / ESLint 一致性（执行层面）

- 格式化以 **Prettier** 为准；Lint 以 **ESLint recommended 集**为准；二者冲突时以 **`eslint-config-prettier`** 为准。
- 提交前变更应可通过 **`npm run lint`** / **`npm run lint:fix`** 与 **`npm run format`** 修复，不在业务代码中关闭规则除非有文件级、有理由的例外并附注释。

---

## AI 生成代码时的优先顺序

1. 满足本文件架构与命名约束。  
2. 满足 Vue / TS 规则与 FSD 分层边界。  
3. 与当前仓库 ESLint / Prettier / Commitlint 配置不冲突。  
4. 仅在必要时补充最小注释（解释「为什么」，非「做什么」）。
